# Runtime deps for the exported IK engine. Install on the deployment
# target with `pip install -r requirements.txt`. python-fcl is included
# unconditionally so [ik].collision_aware can be flipped at deploy time
# without re-exporting.

numpy>=1.26
lxml>=5.0
pydantic>=2.6
tomli_w>=1.0
protobuf>=4.0
aiohttp>=3.9
python-fcl>=0.7
scipy>=1.10
# Used at engine startup to compute per-link mesh-centroid TCP offsets, so
# clients that don't know mesh geometry (the control bridge, joysticks) get
# centroid-anchored rotation for free.
trimesh>=4.0
