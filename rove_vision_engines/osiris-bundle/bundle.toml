# Osiris bundle metadata (generated)
[bundle]
created = "2026-06-10T04:52:43Z"
engines = ["rt_detr_coco", "rt_detr_pose", "trained_67f67256"]

[host]
# OS/arch the prebuilt binary targets; run.sh rebuilds from source on a mismatch.
os = "linux"
arch = "x86_64"

[artifacts]
prebuilt_binary = true
webrtc_gateway = true
